name: CI

on:
  push:
    branches: [main, master]
  pull_request:
    branches: [main, master]

jobs:
  test:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        node-version: [20, 22]

    steps:
      - uses: actions/checkout@v4

      - name: Use Node.js ${{ matrix.node-version }}
        uses: actions/setup-node@v4
        with:
          node-version: ${{ matrix.node-version }}
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Type check
        run: npm run typecheck

      - name: Run tests
        run: npm test

      - name: Build
        run: npm run build

      # Publishability canary — closes the architect-review gap that
      # caused d8f6b06 → v0.3.8 to fail at publish-time. `npm publish
      # --dry-run` from a clean checkout (no sibling-repo trickery)
      # validates that every dependency in package.json resolves from
      # the public registry. If a future PR introduces another `file:`
      # dep, an unfulfillable workspace dep, or a private-registry dep,
      # this step fails on the introducing PR — not on the next
      # release attempt.
      - name: Publishability canary (npm publish --dry-run)
        run: npm publish --dry-run

  smoke-test:
    needs: test
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [ubuntu-latest, macos-latest]
        node-version: [20]

    steps:
      - uses: actions/checkout@v4

      - name: Use Node.js ${{ matrix.node-version }}
        uses: actions/setup-node@v4
        with:
          node-version: ${{ matrix.node-version }}
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Build
        run: npm run build

      - name: Pack tarball
        run: npm pack

      - name: Install globally from tarball
        run: |
          npm install -g --prefix "$HOME/.local" ./myndhyve-cli-*.tgz
          echo "$HOME/.local/bin" >> "$GITHUB_PATH"

      - name: Verify CLI version
        run: myndhyve-cli --version

      - name: Verify CLI help
        run: myndhyve-cli --help

      - name: Verify dev doctor (JSON mode)
        run: myndhyve-cli dev doctor --format json

  # Tier 0.3 — CLI integration tests against the Firebase emulator suite.
  # Stub today; full implementation lands when the first emulator-driven
  # CLI flow stabilizes (likely `workflows runs <runId>` against a seeded
  # emulator dataset). SLO target: <5min end-to-end.
  emulator-integration:
    needs: test
    runs-on: ubuntu-latest
    if: github.event_name == 'pull_request'  # PR only until the harness stabilizes
    timeout-minutes: 10

    steps:
      - uses: actions/checkout@v4
        with:
          path: myndhyve-cli

      # Sibling checkout for the main repo's Firebase emulator config
      # (firebase.json + .emulator-fixtures/). The CLI itself no longer
      # depends on a sibling checkout — only this integration job needs
      # it for the emulator runtime artifacts.
      - name: Checkout sibling main repo (emulator config)
        uses: actions/checkout@v4
        with:
          repository: myndhyve/myndhyve
          path: myndhyve
          ref: main

      - name: Use Node.js 20
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
          cache-dependency-path: myndhyve-cli/package-lock.json

      - name: Install Firebase CLI
        run: npm install -g firebase-tools

      - name: Install CLI dependencies
        working-directory: myndhyve-cli
        run: npm ci

      - name: Build CLI
        working-directory: myndhyve-cli
        run: npm run build

      - name: Start Firebase emulator (auth + firestore + functions)
        working-directory: myndhyve
        run: |
          firebase emulators:start --only auth,firestore,functions \
            --project demo-myndhyve-cli-test \
            --import=./.emulator-fixtures \
            > "$RUNNER_TEMP/emulator.log" 2>&1 &
          # Wait for emulator UI to come up (port 4000 by default).
          for i in {1..30}; do
            if curl -fs http://localhost:4000/ > /dev/null; then
              echo "Emulator up after $i attempts"
              break
            fi
            sleep 2
          done

      - name: Run CLI integration smoke
        working-directory: myndhyve-cli
        env:
          FIREBASE_AUTH_EMULATOR_HOST: localhost:9099
          FIRESTORE_EMULATOR_HOST: localhost:8080
        run: |
          # TODO(Tier 0.3 follow-up): replace with real `myndhyve-cli workflows ...`
          # commands once the emulator fixtures land. For now this just asserts the
          # CLI binary launches against an emulator-aware environment.
          node dist/index.js --version
          node dist/index.js --help

      - name: Dump emulator log on failure
        if: failure()
        run: cat "$RUNNER_TEMP/emulator.log" || true
